# Production build env vars (committed, public values only).
# Loaded by Vite during `npm run build` and `npm run preview`.
VITE_API_BASE_URL=https://collectcore-production.up.railway.app
VITE_IS_ADMIN=true
